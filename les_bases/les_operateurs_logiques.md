# Les opérateurs logiques


- ET logique : ```condition && autreCondition```
- OU logique : ```condition || autreCondition```
- NON logique : ```!condition```
- Les parenthèses : ```condition || (autreCondition && !uneAutreCondition)```

Ces opérations retournent ```true``` ou ```false```.
